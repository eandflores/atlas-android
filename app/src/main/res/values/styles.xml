<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>

        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="android:textColorHint">@color/color_primary</item>
        <item name="colorControlNormal">@color/color_primary</item>
        <item name="colorControlActivated">@color/color_accent</item>
    </style>

    <style name="toolbar_dark" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/color_toolbar_text</item>
        <item name="android:textColorSecondary">@color/color_toolbar_icons</item>
        <item name="actionMenuTextColor">@color/color_toolbar_icons</item>
    </style>

</resources>
